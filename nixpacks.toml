[phases.setup]
nixPkgs = [
  "nodejs_20",
  "python3",
  "pkg-config",
  "cairo",
  "pango",
  "libjpeg",
  "giflib",
  "librsvg",
  "pixman",
  "glib",
  "fontconfig",
]

[phases.build]
cmds = ["npm install --omit=dev", "npm rebuild canvas --build-from-source"]

[start]
cmd = "node start.js"
