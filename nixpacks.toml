[phases.setup]
nixPkgs = [
  "nodejs_20",
  "python3",
  "pkg-config",
  "cairo",
  "pango",
  "libjpeg",
  "giflib",
  "librsvg",
  "pixman",
  "glib",
  "fontconfig",
  "util-linux",
  "libuuid",
]

[phases.build]
cmds = ["npm install --omit=dev"]

[start]
cmd = "node start.js"
